# Do not include azure-functions-worker in this file
# The Python Worker is managed by the Azure Functions platform
# Manually managing azure-functions-worker may cause unexpected issues

azure-functions
pydantic==2.13.4
azure-communication-email==1.1.0
azure-cosmos==4.16.3
azure-storage-blob==12.30.0
azure-storage-file-datalake==12.25.0
azure-storage-queue==12.17.0
azure-mgmt-containerregistry==15.0.0
azure-mgmt-web
azure-batch==14.2.0
azure-core==1.41.0
azure-identity==1.25.3
email_validator==2.3.0
psycopg2-binary==2.9.12
requests==2.34.2
requests-auth==8.0.0
rasterio==1.3.11
opencv-python==5.0.0.93
GDAL @ https://github.com/microsoft/haste/releases/download/haste-binaries/GDAL-3.9.2-cp311-cp311-manylinux_2_17_x86_64.manylinux2014_x86_64.whl ; sys_platform == 'linux'
shapely==2.1.2
pyyaml==6.0.3
# Planetary Computer publishing (vendored REST client) — geo stack used by the
# PC provider's STAC builder (lazy-loaded when the API resolves the PC target
# to validate a publish request). Keep pystac < 1.12: 1.12+ defaults to
# emitting STAC 1.1.0, but MPC Pro requires STAC 1.0.0 (stac.py STAC_VERSION).
pystac[validation]==1.11.0
geopandas==1.1.2
pyproj==3.6.1
pyogrio==0.10.0
boto3==1.43.73
tensorboard==2.19.0
tenacity==9.1.4
typing-extensions==4.16.0
docker==7.2.0
# hastegeo install source -- the local/remote toggle lives here, not in the
# Dockerfile. Default is the editable local tree so docker-compose builds from
# the checked-out source with no wheel publish. CI rewrites this to the wheel
# line below when building deploy images.
-e ../../hastelib
# hastegeo @ https://github.com/microsoft/haste/releases/download/haste-binaries/hastegeo-1.0.26-py3-none-any.whl

# Pin below 6.x: the Azure Functions Python worker bundles protobuf
# 5.29.x and its generated pb2 modules fail to load under 7.x, which
# makes the worker report "0 functions found" with no error.
protobuf==5.29.6
